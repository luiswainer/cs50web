name = "ds"
#first char of name:
print(name[0])

#list ( sequence of mutable values)
names = ["Harry", "Ron"]
print(names[0])

#tupples (sequence of unmutable values (do not change))
coordinate = (10.0, 20.0)
print(coordinate)

#set (collection of unique values)

#dict(dictionary, collection of key-value pairs)
